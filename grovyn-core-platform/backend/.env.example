# Server
NODE_ENV=development
PORT=3001

# CORS: comma-separated allowed origins (e.g. your Netlify frontend URL).
# Leave empty or set to * for development (allows any origin).
# Production (Vercel): set to https://your-app.netlify.app
# CORS_ORIGIN=
# Example for production:
# CORS_ORIGIN=https://grovyn-autopilot.netlify.app
