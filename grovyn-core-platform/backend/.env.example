# Server
NODE_ENV=development
PORT=3001

# CORS: comma-separated allowed origins. Production (Vercel) default: Netlify app + http://localhost:5173
# Override to add/change origins, e.g. CORS_ORIGIN=https://app.netlify.app,http://localhost:5173
# CORS_ORIGIN=
