# Server
NODE_ENV=development
PORT=3001

# CORS: comma-separated allowed origins. Production (Vercel) default: Netlify app + http://localhost:5173
# CORS_ORIGIN=

# Session tokens (stateless HMAC). Required for auth on Vercel serverless. Set in production.
# SESSION_SECRET=your-random-secret-at-least-32-chars
