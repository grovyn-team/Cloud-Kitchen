# TASK BOARD

> Owned by Project Master. Every other agent updates its own row's status when it
> finishes work — do not edit another agent's active row. Keep entries short;
> detailed reasoning goes in DECISIONS_LOG.md.

## How to use
- Status values: `Backlog → Planned → In Progress → In Review (Security) → In Review (DB) → In Review (Critic) → Done → Blocked`
- **Every row cites an Outcome** (O1 See / O2 Run / O3 Plan / O4 Trust). Infra
  rows cite the outcome they unblock; if a row maps to none, it's flagged.
- One row per unit of work (a module slice, not a whole module at once).
- `Blocked` rows must state what/who they're blocked on.
- Any decision row reaches `Done`/`Accepted` only after a `decision-critic` review.

## Phase 0 — Audit & stabilization

| ID | Outcome | Module | Task | Owner Agent | Status | Depends On | Notes |
|----|---------|--------|------|-------------|--------|------------|-------|
| P0-01 | O4 | Foundation | Audit existing backend + frontend (file-level) | Project Planner | **Done** | — | `AUDIT_PHASE0.md` |
| P0-02 | O4 | Foundation | Phase plan (P1 detailed, later summary) | Project Planner | **Done** | P0-01 | `PHASE_PLAN.md` |
| P0-03 | O4 | Foundation | Propose tenancy model + DB engine | Database Administrator | **Accepted (amended)** | P0-01 | D-001; RLS pulled into Phase 1 |
| P0-04 | O4 | Foundation | Propose ORM choice | Database Administrator | **Accepted — Drizzle** | P0-03 | D-002; resolved by P1-00 RLS-pooling spike (`spikes/p1-00-rls-pooling/SPIKE.md`) |
| P0-05 | O4 | Foundation | Baseline OWASP review of inherited code | Security Engineer | **Done** | P0-01 | `SECURITY_REVIEWS/000-baseline-owasp.md`; SEC-01..04 close in P1 |
| P0-06 | O4 | Foundation | Fix broken `npm run verify` (missing password) | Backend Developer | **Done*** | — | D-003. *Runtime PASS re-confirm pending `npm install` |
| P0-07 | — | Foundation | Vite 5→8 bump — **RE-OPENED**, isolate + review | Frontend Developer + Security | **Backlog** | — | D-004 superseded: bump now exists uncommitted; build+smoke+audit+security review before merge. **Carried standing rec (SECURITY_REVIEWS/001+002):** wire `npm audit` as a CI gate for **both** backend + frontend (concrete target = drizzle-kit dev-only esbuild advisories); one gate, both package trees. |
| P0-08 | O4 | Foundation | Populate TASK_BOARD with P0/P1 | Project Master | **Done** | P0-02 | this file |
| P0-09 | governance | Foundation | Adopt O1–O4 + register `decision-critic` (frontmatter fix) | Project Master | **In Progress** | — | Agent file had no YAML → never registered; §A.1 of sign-off plan |

## Phase 1 — Persistence + Tenancy + Auth (O4; unblocks everything). Auth first.

| ID | Outcome | Module | Task | Owner Agent | Status | Depends On | Notes |
|----|---------|--------|------|-------------|--------|------------|-------|
| P1-00 | O4 | Foundation | RLS-pooling spike: `SET LOCAL app.current_tenant` in a pooled txn, Prisma **and** Drizzle; pick ORM | Database Administrator | **Done — critic gate SATISFIED (CRITIQUE 015)** | D-006 | Resolves D-002: **Drizzle wins** (both pass all 5 binary gates; Drizzle passes the promoted hard gate 7 — native `pgPolicy()`/`.enableRLS()` auto-generates RLS SQL, Prisma requires 100% hand-written SQL). Evidence: `spikes/p1-00-rls-pooling/SPIKE.md`. **Independent re-review complete (CRITIQUE 015): ENDORSE Drizzle, gate satisfied, P1-01 unblocked.** One Significant correction carried to **P1-02** (not P1-01): set tenant context via `set_config('app.current_tenant', $1, true)` bound-parameter — spike's "raw interpolation unavoidable" claim is false; UUID validation is defense-in-depth only |
| P1-01 | O4 | Foundation | Minimal migrations: tenant, user, session, audit_log; **RLS enabled**; plan/branch_limit/seat_limit + soft-delete/retention fields | Database Administrator | **Done** | P1-00 | Non-null indexed `tenant_id`; retention baked in (D-008/D-009). `user` PII: soft-delete + retention field only, **not** crypto-shred (D-013). Schema: `backend/src/db/schema.js`; migrations: `backend/drizzle/0000_conscious_namor.sql` (auto-gen tables+ENABLE+POLICY), `0001_force_rls_and_grants.sql` (hand-written FORCE+GRANT), `bootstrap-roles.sql` (cluster-wide role creation, run once, outside the versioned chain). **Both CRITIQUE-015 verification items closed with real evidence (not by inspection)**: (1) `pgPolicy({using, withCheck})` set explicitly on every policy — generated SQL and live `pg_policies.with_check` both confirm it; a real cross-tenant INSERT against a throwaway `postgres:16-alpine` container was rejected with "new row violates row-level security policy", a same-tenant INSERT succeeded — see D-014. (2) Ran a real second-pass `generate` after altering an existing policy predicate + adding a new tenant-scoped table: drizzle-kit emitted a correct incremental `ALTER POLICY` (not drop+recreate) and a correct fresh `CREATE POLICY ... WITH CHECK` on the new table; applied live and confirmed via `pg_policies`. Logged as D-014. **D-014 is Accepted per `CRITIQUES/016-d014-p1-01-schema-rls.md` (ENDORSE WITH CHANGES / Significant — critic gate SATISFIED).** Critic gate is now cleared; the remaining Definition-of-Done gate is the **security-engineer OWASP pass** — **now DISCHARGED**. Two review files exist: `001-p1-01-schema-rls.md` (self-produced first pass — CLEARS the security gate, no Critical/High, 4 forward-looking findings) and `002-p1-01-independent-review.md` (**genuinely independent countersign** that verified every A01/A03/A08 claim directly against the SQL + `_journal.json`/snapshot files rather than inheriting 001 — verdict **CONCUR, no Critical/High, CLEAR WITH FOLLOW-UPS**). **002 is the pass that discharges the gate** (001 lacked separation of duties by its own admission). **All four DoD gates satisfied: planning ✓ · schema/migration ✓ · critic ✓ (D-014 Accepted, CRITIQUE 016) · security ✓ (002 independent). → DONE.** CRITIQUE 016 registered **5 non-optional forward-spec follow-ups** now tracked on other rows (none edit P1-01's own artifacts except one DBA-owned comment fix, below): pre-context read path → P1-02/P1-04; audit_log crypto-shred → P1-13; fail-closed `retain_until` → P1-13/purge; `rolbypassrls=false` assertion + idempotent bootstrap → P1-10; audit correction escape-hatch runbook → P7-01/P7-02. **DBA follow-up on this file:** correct the misleading `tenant.slug` comment (`backend/src/db/schema.js` ~lines 170-174) — it claims slug lookup works "before a tenant context exists," which is false for `grovyn_app` under RLS (Minor doc fix, does not block Done — carried to P1-02's DBA sub-task). **SECURITY_REVIEWS/002 added 3 new forward follow-ups (IR-01→P1-10, IR-02→P7-02, IR-03→P1-02) + carried the npm-audit CI-gate rec, all registered on their home rows below.** |
| P1-02 | O4 | Foundation | RLS context middleware + **BYPASSRLS role** (migrations/seed) | Database Administrator + Backend | **Done** | P1-01 | Per-request tenant GUC; backstop for a forgotten scope. **Set context via `set_config('app.current_tenant', $1, true)` with a BOUND PARAMETER — not raw string interpolation; UUID validation is defense-in-depth (CRITIQUE 015, non-optional)**. **NEW — SEC-P101-IR-03 (Info, SECURITY_REVIEWS/002, forward-flag for P1-02's review):** this review MUST confirm the runtime pool uses `grovyn_app` with **NO fallback to the BYPASSRLS `grovyn_migrator` URL** in any deployed environment (the two roles must be genuinely distinct everywhere — a runtime accidentally on the migrator silently dissolves ALL RLS), and that the `set_config` GUC is **transaction-scoped-correct under connection pooling** (the `true`/is_local arg + txn boundaries survive pool checkout/checkin). If tenant-resolution uses a `SECURITY DEFINER` fn, its `search_path`/body must be locked down and reviewed here. **NEW — own the pre-context read path (CRITIQUE 016 §C.1, non-optional):** authentication is inherently pre-context (resolve tenant from subdomain/slug, then user by email) but `grovyn_app` reads ZERO rows from `tenant`/`user` with no context set, and the migrator role is request-forbidden — so decide + record HOW a request resolves its tenant and authenticates before context exists (narrow SELECT-only `grovyn_auth` role scoped to tenant-resolution+login, OR `SECURITY DEFINER` resolver fns like `resolve_tenant_by_slug()`/`authenticate()`). This is a **prerequisite for P1-04**, lands here. **DBA sub-task:** correct the misleading `tenant.slug` comment in `backend/src/db/schema.js` (~lines 170-174) that claims slug lookup works before tenant context (Minor doc fix). **— SEQUENCING (Project Master, 2026-07-29, In Progress):** decomposed into 4 ordered sub-tasks, each with an owner and a required gate; architecturally item 2 (pre-context read path) precedes item 1 (context wiring) because a request must resolve its tenant before it can set context for it. **(S1) DBA — pre-context read path (CRITIQUE 016 §C.1) — DONE, pending critic review.** Chose **`SECURITY DEFINER` resolver functions** (`resolve_tenant_by_slug(slug)`, `authenticate_lookup(tenant_id, email)`) over a narrow `grovyn_auth` role — logged as **D-015** (`.claude/DECISIONS_LOG.md`), status Proposed/awaiting decision-critic (same pattern D-014 used). Implemented in `backend/drizzle/0002_pre_context_auth_resolvers.sql` (owned by `grovyn_migrator`, `EXECUTE`-only grant to `grovyn_app`, `search_path` locked to `''`, fully-qualified identifiers). Verified with real evidence against a throwaway `postgres:16-alpine` container: 8/8 functional checks pass (fail-closed default path unaffected; slug/email resolution works pre-context; cross-tenant `authenticate_lookup` attempt returns 0 rows; soft-deleted user excluded) **plus a real search-path-hijack test** proving `SET search_path = ''` defeats a poisoned caller search_path that *does* successfully hijack an ordinary unqualified query in the same session. Also fixed the misleading `tenant.slug` comment in `backend/src/db/schema.js` (~lines 170-186) to describe the actual resolver-function mechanism instead of a nonexistent direct-query path. Self-serve tenant *creation* remains explicitly out of scope (deferred). S1 needs decision-critic gate before D-015 moves to Accepted. **(S2) Backend — set_config middleware — DONE, pending critic/security review.** Built `backend/src/middleware/tenantContext.js`: `withTenantContext(pool)` is a handler-wrapping higher-order function (`(req, db) => result`, not a bare `(req,res,next)` middleware attaching `req.db` + `res.on('finish')` commit) — COMMITs/ROLLBACKs and releases the connection **before** any response byte is sent, using `set_config('app.current_tenant', $1, true)` with a **bound parameter** (CRITIQUE 015, non-optional). UUID-shape validation (`isValidTenantId`) is defense-in-depth only, gating with a 400 before any query runs. Handler receives only a frozen `{ query }` object bound to the checked-out client — never the pool, never `res` — structurally preventing a future handler from bypassing context-setting. `req.tenantId` is documented as P1-04's responsibility to set from a verified server-side session, never client input; nothing here invents a fake auth layer. **(S3) Backend — runtime pool — DONE, pending critic/security review.** Built `backend/src/db/pool.js`: net-new (no runtime DB module existed before this task — `backend/src/db/` held only `schema.js`). Reads **only** `DATABASE_APP_URL`, authenticates as `grovyn_app` (NOBYPASSRLS), **zero fallback** to `DATABASE_URL`/`DATABASE_MIGRATOR_URL` under any circumstance (closes SEC-P101-IR-03) — throws at import time if unset, verified via a child-process probe. `backend/.env.example` updated: `DATABASE_APP_URL` documented as its own distinct variable, not reusing `DATABASE_URL` (logged as D-016 — reusing `DATABASE_URL` would have made it ambiguous between "the runtime app connection" and "the CLI's migrator fallback", undermining IR-03's "genuinely distinct roles everywhere" by naming rather than by construction). Neither `app.js`/`server.js`/`routes/v1/index.js` were touched — nothing wires this into the live, running app yet (that's P1-03/P1-04's job); this task's proof is a standalone real-Postgres test suite, not a live unauthenticated diagnostic route (mounting one would itself be an SEC anti-pattern — trusting a client-supplied tenant id). **Verification (real, not by inspection):** `backend/tests/tenantContext.pgtest.mjs` (20 checks, plain Node + `node:assert`, run via `npm run test:tenant-context`) against a real throwaway `postgres:16-alpine` container with 0000→0002 + `bootstrap-roles.sql` applied and 2 tenants/1 user each seeded — tenant self-access + cross-tenant `user` isolation via real HTTP calls through wrapped handlers; fail-closed on missing/malformed tenant id; handler-throw → ROLLBACK + generic 500 (no stack trace) + pool recovery; no GUC leak to a fresh checkout post-commit; a real pool-oversubscription concurrency run (pool=3, 24 concurrent requests across 2 tenants, forced overlap via `pg_sleep`) — zero cross-tenant leaks, confirmed genuine connection reuse via `pg_backend_pid()`; a direct SQL-injection-shaped payload through the bound `set_config` parameter proven inert (stored as a literal, `tenant` table intact); pool fail-closed-at-import confirmed both directions. `npm run verify` (existing DB-free suite) re-confirmed passing unmodified — purely additive. New D-016 logged (env-var/middleware-shape choices), status Proposed/awaiting decision-critic, same gate pattern as D-015. **(S4) Gates — do NOT skip, NOT started:** decision-critic pass on D-015 (S1) **and** D-016 (S2/S3) **AND** independent security-engineer review confirming IR-03 (no runtime→migrator fallback — now built and verified, needs independent countersign), distinct roles everywhere, `SECURITY DEFINER` body/`search_path` locked (S1, unchanged), GUC pooling-safe under real contention (now built and verified, needs independent countersign). Status flow: In Progress → In Review (Critic) → In Review (Security) → Done. **Blocked-in-context:** the coordinating project-master session cannot itself author schema/SQL/middleware (guardrail) nor spawn the DBA/backend/critic/security subagents from here — these must be spawned separately (the way P1-01's CRITIQUE 016 + SECURITY_REVIEWS/002 were). S4 (decision-critic, then security-engineer) is the next spawn. **— CLOSE-OUT (Project Master, 2026-07-29): all four DoD gates satisfied → DONE.** planning ✓ · schema/middleware/pool ✓ (S1/S2/S3) · critic ✓ (**D-015 Accepted per `CRITIQUES/017-d015-pre-context-auth-resolvers.md`**; **D-016 Accepted per `CRITIQUES/018-d016-context-middleware-pool.md`** — both ENDORSE WITH CHANGES / Significant, gates SATISFIED) · security ✓ (**independent DoD pass `.claude/SECURITY_REVIEWS/003-p1-02-context-middleware-pool.md` — CLEAR WITH FOLLOW-UPS, no Critical/High**; IR-03 no-fallback pool, bound-parameter `set_config` inertness, transaction-scoped GUC with connection-discard-on-error all independently verified). Four open follow-ups from SR3 land on forward rows (none block this Done, nothing wired into the live app yet): **SR3-01** (Medium) + **SR3-02** (Low) + **SR3-04** (Info) → P1-03; **SR3-03** (security-adjacent) reinforces P1-04. Same close pattern as P1-01. |
| P1-03 | O4 | Foundation | Fail-closed scoped DAL wrapper (missing scope throws) | Database Administrator + Backend | **Done** | P1-02 | **Primary** enforcement; RLS is the backstop. Build on P1-02's `withTenantContext` (`backend/src/middleware/tenantContext.js`). **Full attached scope from SECURITY_REVIEWS/003 + CRITIQUE 018 — address as ONE pass, not separately:** **(1) SR3-01 (Medium, A05/availability — BLOCKING PREREQUISITE, must close BEFORE the first live route mounts):** `backend/src/db/pool.js` sets no `statement_timeout`, `connectionTimeoutMillis`, or `query_timeout` — everything defaulted (max=10, idleTimeout=10000). On the single-box D-006 target one tenant's wedged/slow query holds its pooled connection until OS TCP timeout, and with no `connectionTimeoutMillis` every OTHER request's `pool.connect()` queues indefinitely — a real cross-tenant DoS coupling (the P1-04 login path especially needs `connectionTimeoutMillis` so pool exhaustion errors instead of hanging login), not a sizing nicety. Set these before any route goes live. Owner: DBA/backend here; deploy config re-affirmed at P7. **(2) SR3-04 / CRITIQUE 018 F1 (Info, correctness-only — reversibility window open NOW):** replace the `{status,body}` duck-typed response envelope (`tenantContext.js:134` `'status' in result && 'body' in result`) with an explicit tagged/branded envelope or a `reply(status, body)` helper before real handlers proliferate. Confirmed **no security dimension** (SR3-04) — a thin availability/correctness footgun only; do it for correctness while cheap. **(3) SR3-02 (Low, A01 defense-in-depth — hardening):** happy-path `release()` recycles the connection with no server-side reset, so a buggy handler's session-level `SET`/`set_config(...,false)`/temp table/advisory lock/prepared stmt can ride a recycled connection to the next tenant. Tenant isolation itself is UNAFFECTED (verified — every request re-runs `set_config('app.current_tenant',$1,true)` at BEGIN, an `is_local` setting overrides any lingering session value, and `grovyn_app` is NOBYPASSRLS/non-owner so escalation fails closed); this is non-tenant session hygiene. Add `DISCARD ALL` (or a fixed `RESET ALL`) on connection release as belt-and-braces. **Standing (A09):** the downstream Express error handler P1-03 mounts must stay generic — no `err.message`/stack to the client (a pg error from a failed `set_config`/BEGIN could otherwise leak internal SQL/state). **— STATUS (Backend, 2026-07-29): DONE. Fast-mode per user directive: no DECISIONS_LOG entry, no CRITIQUES file, no security-engineer review gate for this task (auth/upload/money-tax remain the only gated reviews going forward).** Built `backend/src/db/dal.js` (`createScopedDb(client)` — wraps `drizzle-orm/node-postgres`'s `drizzle(client, {schema})` bound to the already-BEGIN+set_config'd client `withTenantContext` hands it, plus a `.raw(text,params)` escape hatch for parameterized SQL; never called except from `tenantContext.js`, and never on an unscoped client) rather than reinventing a query builder — fail-closed-by-construction holds because the DAL never has a code path that produces a query-capable object before context is set, and RLS applies transparently to whatever SQL Drizzle emits, so there's no "forgot the tenant filter" class of bug even with a handwritten `.where()`. Wired into `tenantContext.js` in place of the old bare `{query}` object. All three attached items closed in the same pass: **SR3-01** — `pool.js` now sets `connectionTimeoutMillis: 5s`, `statement_timeout: 15s`, `query_timeout: 20s`, plus `idle_in_transaction_session_timeout: 30s` (bonus, catches a handler stalling mid-transaction between queries, which `statement_timeout` alone can't) — each with an inline one-line-plus rationale comment, values verified live via `SHOW` against a real connection. **SR3-04/F1** — duck-typed envelope replaced with an exported `reply(status, body)` helper branded via a module-private `Symbol`; `tenantContext.js` now checks `isReplyEnvelope()` instead of `'status' in result && 'body' in result`. **SR3-02** — `DISCARD ALL` runs before `client.release()` on the happy path; if `DISCARD ALL` itself throws, the connection is discarded (`release(err)`) instead of recycled, but the already-committed response still goes out normally. Files: `backend/src/db/dal.js` (new), `backend/src/middleware/tenantContext.js` (edited — import + `createScopedDb`, `reply`/`isReplyEnvelope`, `DISCARD ALL`), `backend/src/db/pool.js` (edited — timeouts), `backend/tests/tenantContext.pgtest.mjs` (edited — `db.query`→`db.raw` rename only, no behavior change), `backend/tests/dal.pgtest.mjs` (new, `npm run test:dal`), `backend/package.json` (new script). **Verified (real, not by inspection):** stood up a fresh throwaway `postgres:16-alpine` container, applied 0000→0002 + `bootstrap-roles.sql` **as `grovyn_migrator`** (not superuser — this task discovered the sibling suite's original container notes under-specified this: migrations must run as the migrator role so it owns the tables the GRANTs target, or `grovyn_app`/`grovyn_migrator` both hit `permission denied`), seeded 2 tenants + 1 active user each. Existing `tenantContext.pgtest.mjs` (20 checks) reruns green unmodified in behavior. New `dal.pgtest.mjs` (9 checks): Drizzle `.select().from(schema.tenant)` and the relational `.query.user.findMany()` API both tenant-isolated with **zero app-written tenant filter** (D1.1/D1.2); an explicit cross-tenant `.where(eq(email, otherTenantsEmail))` still returns zero rows (D1.3, proves RLS backstop applies under the builder, not app-layer filtering doing the work); `reply()` branded envelope vs. a plain `{status,body}`-shaped data object sent as-is (D2.1/D2.2, the exact F1 scenario); and — sanity-checked with a **negative control** (temporarily reverted the `DISCARD ALL` line, reran, confirmed D3.2 fails with the leaked value, then restored and reconfirmed green) — a session-level `SET` from one tenant's request does NOT survive onto the same recycled connection (`max:1` pool) for the next request (D3.2, real SR3-02 proof, not by inspection). `npm run verify` (DB-free suite) reconfirmed passing unmodified. Container torn down after verification. |
| **P1-04** | O4 | Auth | ▶ **Real auth**: hashed pw (argon2id/bcrypt), tenant token + expiry/rotation, mandatory `SESSION_SECRET` (fail boot), gated demo pw, fix `AUTH_DEMO_PASSWORD` mismatch | Backend Developer | **Done** | P1-03 | **SECURITY GATE CLEARED (2026-07-29): `.claude/SECURITY_REVIEWS/004-p1-04-auth.md` — independently re-verified via a real throwaway container (measured timing deltas, proved forged-role login body is ignored, proved token rotation invalidates the old token, proved demo routes are structurally 404 when disabled). Verdict CLEAR WITH FOLLOW-UPS, no Critical/High. → all DoD gates satisfied, DONE.** Follow-ups (none block Done): **F2 (Medium, hard prerequisite for P1-05)** — the legacy HMAC `authMiddleware.js` is still mounted on every `/api/v1/*` business route and reads role from a client-held signed token; not exploitable today (demo login is gated off, real sessions don't mint HMAC tokens) but the role-from-DB guarantee proven here does not yet protect real data reads — **P1-05's security review must confirm this legacy path is fully retired**, not just bypassed. F1 (Medium) — no rate-limiting on `/auth/login` yet (argon2 cost is the only brake); track for P1-05/P7 hardening. F3 (Low) — pin argon2 params explicitly rather than relying on library defaults. F4 (Low) — unknown-tenant login failures are console-only, not in `audit_log` (no tenant to attribute the row to); acceptable for now. F5 (Info) — committed `DEMO_PASSWORD` literal, isolated to the gated demo path. **STATUS (Backend, 2026-07-29): implementation DONE, real-Postgres-verified. Fast-mode per user directive: no DECISIONS_LOG entry, no CRITIQUES file for this task's implementation choices — dedicated security-engineer review still required and explicitly NOT done yet (gated separately, per user).** Closes SEC-01/02/04, D-005. Built: **(1)** `src/services/passwordService.js` — argon2id hash/verify + `dummyVerify()` (CRITIQUE 017 timing-oracle fix: a real argon2id verify against a fixed decoy hash runs on every login failure path that didn't already do a real verify). **(2)** `src/db/preContext.js` — the `runPreContextQuery(pool, fn)` helper SR3-03/CRITIQUE 018 F2 asked for: bare `pool.connect()`+`fn`+`release`, no `BEGIN`/`set_config`, used ONLY to call the three pre-context `SECURITY DEFINER` resolvers (D-015's `resolve_tenant_by_slug`/`authenticate_lookup`, plus a new third one — see below); it is the only other place besides `tenantContext.js` that touches `pool`. **(3)** `drizzle/0003_branch_and_staff_access.sql` (auto-gen) + `0004_force_rls_and_grants_branch.sql` (hand-written FORCE+GRANT, mirrors 0001) — minimal `branch` (id, tenant_id, name, timestamps, soft-delete) and `staff_branch_access` (tenant_id, user_id, branch_id, `revoked_at` soft-revoke, partial-unique active-grant index) tables added to `schema.js`, RLS-enabled, same `tenantIsolation()` predicate as every other child table. **(4)** `drizzle/0005_pre_context_session_resolver.sql` — a THIRD `SECURITY DEFINER` resolver, `resolve_session_by_token_hash(token_hash)`, extending D-015's pattern from login to per-request session verification (verifying a bearer token is pre-context for exactly the same reason login is: the server doesn't know the tenant until it looks the token up, and `session`'s RLS policy needs that tenant known first). Joins `user` (role/deleted_at) and aggregates active `staff_branch_access` branch ids in one call — logged as a deliberate, documented extension of D-015's shape rather than a new decision (fast-mode; reasoning is in the migration file's own header comment and this task's report). **(5)** `src/middleware/sessionAuth.js` — `requireSession(pool)` (sets `req.tenantId`/`userId`/`userRole`/`branchIds` from the verified session row ONLY, never client input), `requireRole`, `requireBranchAccess` (ADMIN implicit all-branch, STAFF gated by `req.branchIds`). **(6)** `src/services/sessionService.js` — random 256-bit token, SHA-256 `token_hash` (fast hash, correct choice for a high-entropy machine-generated token, not argon2id); **session design (documented here per fast-mode, no DECISIONS_LOG entry): 24h absolute expiry; refresh is an EXPLICIT `POST /auth/refresh` (not implicit sliding-on-every-request — avoids a DB write per API call and keeps "refreshed" a distinct auditable action per item 9) that ROTATES the token in place (same session id, new secret+expiry, old token immediately dead)**; logout soft-revokes (`revoked_at`), never deletes (D-008/D-013 pattern). **(7)** `src/middleware/tenantContext.js` refactored (behavior-preserving, reverified against the existing 20-check suite unmodified) to extract `runInTenantContext(pool, tenantId, fn)` from `withTenantContext`'s inline transaction logic — reused by login's post-authentication writes (session insert + `auth.login_*` audit row) once a real DB-resolved `tenant.id` is known mid-handler, and by `refresh`/`logout`/`me` (which DO run through ordinary `withTenantContext` once `requireSession` has set `req.tenantId`). **(8)** `src/config/index.js` — `SESSION_SECRET` default removed entirely; **throws at import time if unset** (D-005/SEC-01 closed); added `config.auth.demoModeEnabled` (`AUTH_DEMO_MODE === 'true'`, default false). **(9)** `src/routes/auth.js` rewritten: `login(pool)` (new contract: `{tenantSlug, email, password}`, no `role` field in the request at all — SEC-04 closed structurally, not just by discipline), `refresh(pool)`, `logout(pool)`, `me(pool)`; the OLD demo/seed login (P1-08, unchanged logic) renamed `demoLogin`/`getDemoStoreOptions`, mounted in `routes/v1/index.js` **only when `config.auth.demoModeEnabled`** (route doesn't exist at all when disabled — 404, not a conditional 200/403) — closes SEC-06. **(10)** `src/services/auditService.js` — `logAuditEvent(db, {tenantId, ...})`; auth events audited: `auth.login_success`, `auth.login_failure` (reason tagged `user_not_found`/`invalid_password`; an unknown-tenant-slug failure has no tenant to attribute a row to and is console-logged only, not audit_log — documented tradeoff), `auth.logout`, `auth.session_refresh`. **Role audit (item 6):** grepped `role|STAFF|ADMIN` across `backend/src` before writing any code — confirmed the OLD `routes/auth.js`/`authMiddleware.js` (client-asserted `role` in login body, HMAC-signed stateless token) is the SEC-04 defect; new code never reads `role` from any request, only from `authenticate_lookup`'s DB row (login) or `resolve_session_by_token_hash`'s DB row (every subsequent request via `requireSession`). **Known interim-state consequence (intentional, not a defect):** the OLD legacy dashboard routes (stores/cities/finance/etc.) still run on the OLD in-memory `authMiddleware.js` HMAC-token scheme (P1-05/P1-07 own migrating them to real DB + this session model) — `/auth/login` no longer issues a token those routes accept, so in a production deployment (demo mode off) a real DB-authenticated user cannot yet reach the legacy dashboard endpoints. This is expected given the phased plan (P1-04 auth → P1-05 RBAC middleware → P1-07 ports the routes) and was not bridged, per this task's explicit scope (P1-05/P1-06/P1-07 not built here). `npm run verify` kept green (updated `tests/system.test.js` to call the relocated `/auth/demo-login` and set `AUTH_DEMO_MODE=true`/dummy `SESSION_SECRET`+`DATABASE_APP_URL` for its spawned server — booting now transitively requires both env vars since `routes/v1/index.js` imports `db/pool.js`, but the pool connection itself is lazy so no real Postgres is needed for this DB-free suite). **Verified (real, not by inspection):** real throwaway `postgres:16-alpine` container, migrations 0000→0005 + `bootstrap-roles.sql` applied as `grovyn_migrator`; existing `tenantContext.pgtest.mjs` (20 checks) and `dal.pgtest.mjs` (9 checks) rerun green unmodified after the `tenantContext.js` refactor; new `tests/auth.pgtest.mjs` (`npm run test:auth`, 55 checks, self-seeding fixture) — real login success for ADMIN/STAFF with DB-sourced role (client-sent `role:"ADMIN"` in a STAFF login body proven ignored); `/me` branch-scope reflects `staff_branch_access` exactly; `requireBranchAccess` 200/403 proven for STAFF, 200/200 for ADMIN; full session lifecycle (issue → refresh rotates+invalidates old token → logout revokes, each independently proven with the old token rejected after the fact); direct-SQL-inserted expired/revoked/deleted-user sessions all rejected; all three login-failure causes (unknown tenant, unknown user, wrong password) return byte-identical 401 bodies; **timing-oracle check measures real wall-clock deltas** (12-sample median per branch) — unknown-user vs wrong-password delta asserted `<40ms` (measured ~0.3ms in this run) and both asserted `>=5ms` to prove the dummy/real argon2id verify actually ran (not just "everything's fast so nothing leaks"); audit_log rows confirmed for all four event types with correct `reason` tags; `SESSION_SECRET` unset → config import throws (child-process probe); `AUTH_DEMO_MODE` gate proven as true unreachability (404, two full server boots, not a code-path check). → **security-engineer review is the remaining gate before Done (per this task's explicit fast-mode instruction — do not skip it elsewhere).** |
| P1-05 | O4 | Auth/RBAC | Central tenant + permission + branch middleware; `requireRole`→`requirePermission`, `requireBranchAccess` | Backend Developer | Backlog | P1-04 | Replaces ad-hoc per-handler STAFF filters (SEC-03) → **security review**. **Partial groundwork laid by P1-04 (not this task's scope, just reusable primitives):** `src/middleware/sessionAuth.js` already has `requireSession`/`requireRole`/`requireBranchAccess` working against real DB sessions/`staff_branch_access` — P1-05's own job is a `requirePermission` model (beyond the provisional ADMIN/STAFF role enum) and, per P1-07, actually wiring the legacy dashboard routes (`routes/v1/index.js`'s `adminOnly`/`adminOrStaff` arrays, still on the OLD `authMiddleware.js`) over to this real session model — neither done here. |
| P1-06 | O2 | Foundation | Branch + role/permission tables & mgmt API | Backend Developer + DBA | Backlog | P1-05 | Admin creates branches & staff roles. **Note:** a *minimal* `branch` table (id, tenant_id, name, soft-delete) and `staff_branch_access` (grant table) already exist as of P1-04 (`drizzle/0003`/`0004`) — built only as the FK target/data source P1-04's branch-scope middleware needed. This task still owns real branch CRUD (name edits, address/hours, etc.), a management API, and the real role/permission tables replacing `user.role`'s provisional enum. |
| P1-07 | O2 | Foundation | Port stores/users services to DB, tenant-scoped | Backend Developer | Backlog | P1-03,P1-05 | Proves the foundation on real data. Also the task that resolves P1-04's noted interim gap: the legacy dashboard routes currently authenticate via the OLD in-memory `authMiddleware.js`, not the new DB-backed session model P1-04 built — this task (with P1-05) is where they converge. |
| P1-08 | O4 | Foundation | Gate seed/demo behind env flag, unreachable in prod | Backend Developer | **Done** | P1-01 | Closes SEC-06; keep the demo path, gate it. **Folded into P1-04 (2026-07-29, same commit-set):** `AUTH_DEMO_MODE` env var (`=== 'true'`, default disabled) now gates whether `POST /api/v1/auth/demo-login` / `GET /api/v1/auth/demo-stores` are even mounted in `routes/v1/index.js` — disabled means the routes don't exist (404), not a code path that merely declines. Also fixed the `AUTH_DEMO_PASSWORD` mismatch D-005/the baseline review flagged (`routes/auth.js`'s demo handler now reads `process.env.AUTH_DEMO_PASSWORD`, same as before, still gated). Verified via two real server boots (`AUTH_DEMO_MODE=false`/`true`) hitting the routes directly — see P1-04's verification notes (`tests/auth.pgtest.mjs`, `DemoGate` checks). Demo logic itself is unchanged (still in-memory, still `authMiddleware.js`'s HMAC token) — this task only closes SEC-06's reachability gap, not a rewrite. |
| P1-09 | O2 | Auth/RBAC | Frontend: AuthContext (tenant+permissions), RequireRole→permission gate, branch/staff mgmt UI (empty/loading/error) | Frontend Developer | Backlog | P1-05,P1-06 | Re-derive from real role model. **Backend contract now exists (P1-04, 2026-07-29) for the auth slice specifically** (branch/staff mgmt UI still blocked on P1-05/P1-06 as before): `POST /api/v1/auth/login` body is now `{tenantSlug, email, password}` (no `role` field — role comes back in the response, read from the DB), returns `{sessionToken, expiresAt, tenant:{id,name,slug}, user:{id,email,name,role}}`; `Authorization: Bearer <sessionToken>` on `POST /api/v1/auth/refresh` (rotates token), `POST /api/v1/auth/logout` (204), `GET /api/v1/auth/me` (`{tenant:{id}, user:{id,role}, branchIds:[]}`). This is NOT yet wired to the legacy dashboard endpoints (see P1-04/P1-05/P1-07 notes) — frontend work here still genuinely blocked on P1-05/P1-06 for anything beyond the login screen itself; flagging the contract now so it isn't rediscovered. `API_CONTRACT.md` not updated by this task (backend-only per scope) — flagged for whoever picks up P1-11. |
| P1-10 | O4 | Foundation | Cross-tenant/cross-branch isolation test suite | Security Engineer + Backend | Backlog | P1-05,P1-07 | DoD gate: A can't reach B; staff can't cross branch; prove RLS alone blocks. **NEW (CRITIQUE 016 §B.2, non-optional):** the suite MUST assert `grovyn_app.rolbypassrls = false` against the **real deployed cluster** (carry the P1-00 spike's `pg_roles` check forward as a standing test, not a one-off) — a role silently granted BYPASSRLS dissolves every isolation guarantee, and nothing else catches that drift. **Companion fix:** make `backend/drizzle/bootstrap-roles.sql` idempotent (`DO $$ ... IF NOT EXISTS ... $$` instead of bare `CREATE ROLE`) so review-apps/CI can re-run it safely (Minor). **NEW — SEC-P101-IR-01 (Low, SECURITY_REVIEWS/002, non-optional):** FORCE ROW LEVEL SECURITY and the entire GRANT/privilege model are invisible to Drizzle's snapshot/drift-detection (confirmed: 0 occurrences of "FORCE"/"GRANT" in `meta/0001_snapshot.json` though both are in the hand-written `0001_force_rls_and_grants.sql`) — a future `db:generate` cannot catch their loss. The suite MUST assert these controls LIVE against the cluster (Drizzle can't police them): `pg_class.relforcerowsecurity = true` for all 4 tables; `has_table_privilege('grovyn_app','audit_log','UPDATE') = false` AND `...'DELETE') = false`; plus `DELETE = false` on tenant/user/session. **This is IN ADDITION to the `rolbypassrls=false` assertion already registered here from CRITIQUE 016 — add it, do not replace it.** |
| P1-11 | O1/O2 | Foundation | API contract v1 (auth/tenancy/RBAC) → `API_CONTRACT.md` | Backend Developer + Project Planner | Backlog | P1-04,P1-05 | Frontend builds against this |
| P1-12 | O4 | Foundation | `verify` successor: seed a real test user, drop hardcoded pw | Backend Developer | Backlog | P1-04 | Removes the `AUTH_DEMO_PASSWORD` trap |
| P1-13 | O4 | Foundation | PII-vs-financial erasure **design doc** — **customer PII only** (split master PII vs invoice-snapshot; backup-erasure mechanism) | Database Administrator + Security | Backlog | P1-01 | D-008/D-013; design + user sign-off before **any Phase-3 customer-PII schema**. Depends on P1-01 for the schema *pattern* only — does not gate or block P1-01 itself (user/staff PII is out of crypto-shred scope per D-013). **NEW (CRITIQUE 016 §5/§C.2, non-optional):** the design MUST cover `audit_log` — its `before_data`/`after_data` jsonb snapshots capture customer PII from Phase 3 on, but audit_log is immutable + append-only (no soft-delete, GRANT-blocked from UPDATE/DELETE). Reconcile immutability with erasability via **crypto-shredding the snapshot PII fields** (encrypt per-subject, shred the key — erases the data without mutating the immutable row). **NEW (CRITIQUE 016 §B.3, non-optional — track against the future purge task):** fail-closed `retain_until` — (a) all soft-delete writes go through ONE chokepoint that always sets `retain_until` (mirror D-009's `checkLimit()` single-chokepoint pattern); (b) the purge job MUST treat `deleted_at IS NOT NULL AND retain_until IS NULL` as **do-not-purge-and-alert**, never as safe-to-purge. |

## Phase 2 — Sales + minimal Admin dashboard (O2 → O1 vertical slice)

> Exit: authenticate → upload a sales file → see real revenue across branches on a
> dashboard. Thin slice first; margin/profit tiles fold in as Inventory lands.

| ID | Outcome | Module | Task | Owner Agent | Status | Depends On | Notes |
|----|---------|--------|------|-------------|--------|------------|-------|
| P2-01 | O2 | Sales | Sales schema — `(tenant_id, branch_id, date)` composite index | Database Administrator | **Schema shipped** | P1 done | `sale`+`sale_line_item` in `backend/src/db/schema.js`; migrations `0006_eager_wildside.sql`/`0007_force_rls_and_grants_p2_p6.sql`. Composite `(tenant_id, branch_id, sale_date)` + `(tenant_id, sale_date)` indexes; RLS enabled+forced+verified live (real cross-tenant INSERT/SELECT spot-check). Combined one-pass migration per user's fast-mode directive, covers P2/P3/P3.5/P6 together — see DBA report 2026-07-29. Consumed by P2-02/P2-03 (2026-07-29) — flagged a real gap for DBA follow-up: `branch_id` FKs on `sale`/other P2/P3 tables are single-column, not composite `(id, tenant_id)` like `sale_line_item`'s, closed at the application layer for now (`saleService.branchExistsInTenant`). **Closed at the schema level (DBA, 2026-07-29): migration `backend/drizzle/0009_lovely_morgan_stark.sql`** adds `UNIQUE(id, tenant_id)` on `branch` and re-points `sale.branch_id`/`inventory_item.branch_id`/`customer.branch_id`/`notification.branch_id`/`staff_branch_access.branch_id` to composite `(branch_id, tenant_id) → branch(id, tenant_id)` FKs — real-Postgres-verified (cross-tenant-mismatched insert now rejected by the FK itself on `sale`/`inventory_item`/`customer`/`notification`/`staff_branch_access`); `branchExistsInTenant` left in place as defense-in-depth per the task's instruction, not removed. `tax_period_summary.branch_id` is a **known remaining gap**, explicitly out of this fix's scope (flagged inline in `schema.js`) — needs its own follow-up migration. |
| P2-02 | O2 | Sales | Manual entry + Excel/CSV import (validate-before-commit, row errors) | Backend Developer | **Sec-reviewed: CLEAR w/ follow-ups** | P2-01 | Implementation done, real-Postgres-verified (`backend/tests/sales.pgtest.mjs`, 43/43 checks) — → **security review still required** (CSV formula injection, DoS) before Done, per this task's explicit fast-mode instruction (not skipped). Security review done 2026-07-29 (`.claude/SECURITY_REVIEWS/005-sales-upload.md`) — CLEAR WITH FOLLOW-UPS, no Critical/High, 4 Low follow-ups; no longer blocks Done. |
| P2-03 | O1 | Dashboard | Port metrics/finance service (revenue rollups), tenant-scoped | Backend Developer | **In Review (Security)** | P2-01 | Sales-derivable metrics only. Rollup endpoint (`GET /api/v1/sales/rollup`) implemented + verified against known seeded data; rides the same P2-02 security-review gate (shared upload/auth surface) before Done. **Dashboard aggregation backend (2026-07-30, fast-mode, no separate security gate per user directive):** added `GET /api/v1/dashboard/summary` (ADMIN+STAFF, STAFF response body structurally omits `revenue`/`aov` keys — server-enforced, not UI-hidden) and `GET /api/v1/dashboard/by-branch` (ADMIN-only), reusing `saleService.getRollup`'s real-SQL aggregation style; real-Postgres-verified (`backend/tests/dashboardFinance.pgtest.mjs`, 44/44 checks). |
| P2-04 | O1 | Dashboard | **Thin Admin dashboard**: revenue / orders / AOV / by-branch / day-week-month | Frontend Developer | Done | P2-03 | Margin/profit/health tiles labelled "coming", not faked. **Backend follow-up available (2026-07-30):** dedicated `GET /api/v1/dashboard/summary`/`/dashboard/by-branch` now exist (see P2-03) — frontend-developer still owns wiring them in; today's shipped UI still runs on `/sales/rollup`. **Frontend rewired (2026-07-30):** `Dashboard.tsx` now runs on `dashboard/summary` (day/week/month toggle; revenue/AOV tiles rendered only when the key is structurally present, so STAFF never sees a fake $0 tile) + `dashboard/by-branch` (ADMIN-only, not fetched at all for STAFF) — old AI-mock hero/insights/store-health wiring removed from this page. |
| P2-05 | O2 | Inventory | Inventory schema + manual/Excel add, append-only audit log | Database Administrator + Backend | **Backend implemented (2026-07-30) — security review CLEAR WITH FOLLOW-UPS (see `SECURITY_REVIEWS/006`; 1 Medium XLSX zip-bomb + Lows, none blocking)** | P2-01 | `inventory_item` (catalog+stock, `low_stock_threshold`/`cost_per_unit` for P2-06) + `inventory_movement` (append-only, `audit_log`-pattern GRANT: SELECT/INSERT only, no UPDATE/DELETE even for the owning tenant — live-verified). **Backend half done (2026-07-30):** manual create/edit (`POST`/`PATCH /api/v1/inventory/items`), Excel/CSV bulk import (`exceljs` + `csv-parse`, all-or-nothing, formula-injection-sanitized), sale-triggered auto-decrement wired into `saleService.createSale` (same transaction), low-stock notification trigger with de-dupe, staff request flow (`POST /api/v1/inventory/requests`), branch-scoped list/detail — real-Postgres-verified (`backend/tests/inventory.pgtest.mjs`, 87/87 checks incl. cross-module sale+decrement atomicity). Added `inventory_movement.import_batch_ref` column (`drizzle/0010_gray_zeigeist.sql`). Needs `security-engineer` review (CSV/XLSX upload, tenant/branch scoping) before Done — not skipped, per Definition of Done. `inventory_item.branch_id` composite-FK hardened in `0009_lovely_morgan_stark.sql` — see P2-01 note. **Frontend wired (2026-07-30):** `Operations.tsx` rewired off mock/insights-only content onto the real `/api/v1/inventory/items`\|`/import`\|`/requests` endpoints (branch-scoped list + low-stock filter + item detail w/ movement history + inline edit/stock-adjust, manual add-item form, CSV/XLSX import w/ 3-state success/row-errors/error UI, staff restock-request), reusing Sales.tsx's patterns exactly; `Operations` route/nav opened to ADMIN+STAFF (was STAFF-only) so ADMIN's cross-branch picker is reachable — fast-mode, no security review needed (upload security already reviewed backend-side). |
| P2-06 | O1 | Dashboard | Margin/profit/health tiles (need inventory COGS) | Backend + Frontend | **Backend implemented (2026-07-30), frontend still Backlog** | P2-05 | Completes the executive tiles. **Backend (fast-mode, no security review — rollup of already-stored `sale.tax_amount`, not new tax computation):** `GET /api/v1/finance/summary` (real, ADMIN-only) rebuilt on Sales/Inventory data — revenue, tax collected, and a best-effort partial COGS/gross-margin estimate (`cogs.isPartial`/`costedLineItemCount`/`totalLineItemCount` explicitly flag when not every sale line has an inventory cost link, never presented as authoritative); low-stock + unresolved-notification counts feed `dashboard/summary` tiles (P2-03). **SUPERSEDES the legacy in-memory `/api/v1/finance/summary` mock at the same path** (`routes/finance.js`'s `getFinanceSummary` no longer mounted there — see `routes/financeManagement.js`'s doc comment) — **frontend-developer follow-up required:** `Finance.tsx` currently consumes the old mock shape via `apiPaths.financeSummary` and needs rewiring to the new response shape. Real-Postgres-verified (`backend/tests/dashboardFinance.pgtest.mjs`). **Frontend rewired (2026-07-30):** `Finance.tsx` now consumes the real `finance/summary` shape (revenue/taxCollected/cogs/grossMarginEstimate) with period toggle + branch filter; `cogs.isPartial` surfaced as a visible badge + note, `grossMarginEstimate` always labelled "estimate", never presented as final when partial. Legacy mock store/SKU margin tables (`finance/stores`, `skus/margin-analysis`) dropped from this page — out of this task's real-data scope, still available via their own endpoints if needed later. |

## Phase 3 — Customers + Staff Management (O2)
_Expanded on arrival. Includes staff-removal token revocation (SEC-04 lifecycle)._
**Staff management backend built (2026-07-30, fast-mode):** ADMIN-only account create/list/detail/edit(name+role)/deactivate + branch grant/revoke lifecycle (`backend/src/routes/staffManagement.js` + `backend/src/services/staffManagementService.js`), mounted at `POST/GET /api/v1/staff/accounts`, `GET/PATCH/DELETE /api/v1/staff/accounts/:id`, `POST /api/v1/staff/accounts/:id/branches`, `DELETE /api/v1/staff/accounts/:id/branches/:branchId` — deliberately under `/staff/accounts`, not the brief's literal `/staff`, because that exact path is the still-live legacy workforce-snapshot mock the frontend actually consumes (`apiPaths.staff`) — see the route file's own doc comment. Deactivation soft-deletes the `user` row, revokes all active `staff_branch_access` grants, AND revokes all active `session` rows for that user in one transaction (closes SEC-04 — a removed staff member's existing bearer token is rejected on its very next request, not just on next login). Branch re-grant reactivates the most recently revoked row rather than inserting a duplicate. Simple last-admin guard blocks an ADMIN from self-downgrading/self-deactivating if they're the tenant's last active admin. Real-Postgres-verified (`backend/tests/staffManagement.pgtest.mjs`, `npm run test:staff-management`, 61/61 checks incl. ADMIN-only 403 enforcement, tenant isolation, grant/revoke/re-grant lifecycle, live session-revocation-on-deactivation, last-admin guard, audit_log for all five action types). No security review dispatched per this task's explicit fast-mode gate list (no upload, no money) — flagged as auth-adjacent (session revocation) for awareness, handled defensively, not a substitute for a dedicated review if one is later required. **Frontend built (2026-07-30):** new `StaffManagement.tsx` (route `/staff`, sidebar "Staff", ADMIN-only at both route (`RequireRole`) and nav-visibility level) wired onto the real `/api/v1/staff/accounts` endpoints — paginated accounts list w/ role badge + active-branch-count, add-account form (email/name/password/role/optional initial branch multi-select), row-expand detail panel (edit name/role + grant/revoke branch assignments inline), inline-confirm deactivate surfacing the backend's last-admin-guard message verbatim; the pre-existing legacy `apiPaths.staff` ('/api/v1/staff') workforce-snapshot widget in `Operations.tsx`'s Insights tab is untouched. Fast-mode, no decision-critic/security review needed.
**Customer schema shipped (DBA, 2026-07-29):** `customer` table (branch-scoped, `category`/`rating` per §3) in `backend/src/db/schema.js`, migrations `0006`/`0007`, RLS live-verified. Ordinary D-008 soft-delete only — does **NOT** implement the master-vs-invoice-snapshot split or crypto-shred erasure; **RESOLVED (user, 2026-07-29, D-008 amendment):** backup-expiry, not crypto-shred — erasure window is a P7-02 deployment config value. **Gate lifted (2026-07-30, per D-008 amendment resolution):** real customer PII (name/phone/email) writes are now in production code. **Backend built (2026-07-30):** `POST/PATCH/DELETE /api/v1/customers`, `GET /api/v1/customers`, `GET /api/v1/customers/:id` (`backend/src/routes/customers.js` + `backend/src/services/customerManagementService.js`) — manual create/edit (`audit_log`-attributed), soft-delete-only (`deleted_at`, also the erasure-request path — no crypto-shred, per the resolved decision), branch-scoped paginated list with `category` filter — real-Postgres-verified (`backend/tests/customers.pgtest.mjs`, 49/49 checks incl. tenant/branch isolation and soft-delete exclusion from list/detail). Replaces the legacy in-memory `GET /api/v1/customers` mock at that same path (`routes/v1/index.js` no longer mounts it; `backend/tests/system.test.js`'s no-DB smoke list updated to match, same as Sales/Inventory were never added to it). No security review required (no file upload, no money computation — not a gated category per `PROJECT_BRIEF.md`). `branch` table also extended with address/city/state/postal_code/phone/opening_hours/timezone (P1-06's described scope) in the same migration — P1-06 still owns the branch CRUD API and any real permission-table work; no permission table was added (judgment call: no Phase 2/3/3.5/6 requirement needs finer-grained access than the existing ADMIN/STAFF+branch-scope model). `customer.branch_id` composite-FK hardened in `0009_lovely_morgan_stark.sql` — see P2-01 note. **Frontend wired (2026-07-30):** `RepeatEngine.tsx` (route `/repeat`, sidebar "Customers") rewired off the segments-only mock view onto the real `/api/v1/customers` CRUD (branch-scoped list w/ category filter + pagination, add form, row-expand detail/edit panel, inline confirm-then-delete), reusing Operations.tsx's patterns exactly, with the existing real `/api/v1/customers/segments` AI panel kept as an ADMIN-only tab alongside it; route/nav opened to ADMIN+STAFF (was ADMIN-only) since customer CRUD is a STAFF capability — fast-mode, no decision-critic/security review needed.

## Phase 3.5 — Expansion Planning, deterministic (O3)

> After Customers so margin/revenue/repeat inputs are all real data. No AI.

| ID | Outcome | Module | Task | Owner Agent | Status | Depends On | Notes |
|----|---------|--------|------|-------------|--------|------------|-------|
| P35-01 | O3 | Expansion | Tenant-scope the deterministic expansion/simulator engines on real data | Backend Developer | **Done (backend, fast-mode — no security review needed per this task's directive)** | P3 done | New `src/services/expansionService.js` composes real tenant data (active `branch` count, this-month `saleService`/`financeManagementService` revenue+margin, `notificationService` unresolved count) into the exact shim shapes `expansionPlanner.js`'s pure, unchanged (still 100% deterministic, no AI/HF) scoring/ranking functions already expected — no schema needed beyond the P35-02 `tenant.settings` column below. New `GET /api/v1/expansion/plan` (ADMIN-only, `src/routes/expansion.js`, mounted in `routes/v1/index.js`) — deliberately NOT `/expansion/simulate` (that legacy mock-backed route, `routes/intelligence.js`, is untouched/still mounted). Real-Postgres-verified (`backend/tests/expansion.pgtest.mjs`, `npm run test:expansion`, 32/32 checks) — two tenants with different real branch counts/revenue produce genuinely different `currentStores`/`avgMonthlyRevenuePerStore`/`financials.year1Revenue`; cross-tenant isolation held. `sale` has no `customer_id` in the current schema, so `repeatRate` cannot be computed from real data — exposed as an explicit, tenant/request-overridable input flagged `dataSource.repeatRatePctIsAssumed:true` rather than fabricated (documented in `expansionService.js`, no DECISIONS_LOG entry per fast-mode). |
| P35-02 | O3 | Expansion | Lift hardcoded India cost constants (₹19L/store, 60% COGS, 25% commission, en-IN) into tenant-configurable inputs | Backend Developer + DBA | **Done (backend, fast-mode)** | P35-01 | `expansionPlanner.js`'s `projectFinancials`/`calculateGrovynImpact` now take a 4th `costAssumptions` param (`resolveCostAssumptions()`, defaults = the exact prior hardcoded literals — zero behavior change for a caller passing none). Precedence: request query param > `tenant.settings.expansion` (new nullable jsonb column, migration `0011_damp_tarot.sql` — deliberate small JSON knob, not a dedicated settings table, per this task's own "keep pragmatic" instruction) > engine default. `GET /api/v1/expansion/plan` accepts `cogsPct`/`commissionPct`/`equipmentCostPerStore`/`renovationCostPerStore`/`depositCostPerStore`/`inventoryCostPerStore`/`monthlyRentPerStore`/`monthlyUtilitiesPerStore`/`monthlyStaffCostPerStore`/`currency`/`locale`/`repeatRatePct` overrides, range-validated in the route. Real-Postgres-verified: overriding `cogsPct` changes month-1 `cogs` by the exact predicted delta with `revenue` unchanged; overriding `equipmentCostPerStore` changes `totalSetupCost` by the exact predicted delta; a tenant's stored `settings.expansion.monthlyRentPerStore` is used as the default and a query override still wins over it (precedence proven, not just declared). No DECISIONS_LOG/CRITIQUES entry per this task's explicit fast-mode directive — `tenant.settings` shape choice documented inline in `schema.js`/`expansionService.js` instead. |
| P35-03 | O3 | Expansion | Expansion UI on real data; empty/loading/error states | Frontend Developer | **Done (frontend, fast-mode — no critic/security review per this task's directive)** | P35-01 | `ScaleSimulator.tsx` now calls `GET /api/v1/expansion/plan` (ADMIN-gated at route + in-component, existing loading/error states kept, `dataSource.repeatRatePctIsAssumed` surfaced as an amber caveat banner); existing `components/simulator/*` display components reused unchanged (response shape matched their existing props). |

## Later phases (SUMMARY — expand on arrival)
- **Phase 4 — Full Dashboard + Notifications (O1).** Cross-branch executive view;
  Staff→Admin notifications.
  **Schema shipped (DBA, 2026-07-29):** `notification` table (branch-scoped, `type`/`status` enums, actor + polymorphic `related_entity_type`/`related_entity_id`) shipped early alongside P2/P3/P3.5/P6 in the same combined migration, since low-stock/inventory-request notifications are triggered by the Inventory module. RLS live-verified. `notification.branch_id` composite-FK hardened in `0009_lovely_morgan_stark.sql` — see P2-01 note. **Reader/consumer API built (Backend, 2026-07-30, fast-mode):** `GET /api/v1/notifications` (branch-scoped list, `status` defaults to `unread` when omitted — documented judgment call, no DECISIONS_LOG entry), `GET /api/v1/notifications/unread-count` (badge count), `PATCH /api/v1/notifications/:id/read` (unread→read, idempotent no-op from read/resolved), `PATCH /api/v1/notifications/:id/resolve` (ADMIN-only — router-level `requireRole(['ADMIN'])` layered on top of the shared STAFF+ADMIN auth array; idempotent, reachable directly from unread or read) — `backend/src/routes/notifications.js` + `backend/src/services/notificationService.js`, mounted in `routes/v1/index.js`. Does NOT touch the producer side (`inventoryManagementService.js`'s low-stock trigger / `routes/inventoryManagement.js`'s `createRequest`) — reads the same rows those already write. Real-Postgres-verified (`backend/tests/notifications.pgtest.mjs`, `npm run test:notifications`, 41/41 checks incl. tenant/branch isolation, STAFF-vs-ADMIN resolve gate, unread→read→resolved transitions + idempotency, unread-count accuracy before/after mutation). No security review dispatched per this task's explicit fast-mode gate list (no upload, no money/tax). **Frontend wired (2026-07-30, fast-mode):** `Alerts.tsx` (route `/alerts`) rewired off the old `autopilot/alerts` mock onto the real `/api/v1/notifications` endpoints — status tabs (unread/read/resolved/client-merged "all"), ADMIN-only branch filter (STAFF auto-scoped server-side, filter not even rendered), "Mark read" (both roles, unread items) and "Resolve" (ADMIN-only, gated in the UI not just relying on the backend's 403) actions, pagination; `Sidebar.tsx` gained a polled (60s) unread-count badge next to "Alerts". Old `apiPaths.alerts` (`autopilot/alerts`)/`Alert` type left untouched — still consumed by `Dashboard.tsx`/`Stores.tsx`, a separate feed. No decision-critic/security review needed per this task's explicit fast-mode instruction.
- **Phase 5 — AI narrative layer (O1/O3 support).** HF-wrapped garnish; degrades
  to nothing; never blocks a core response (D-010).
- **Phase 6 — Tax Assistant (O4).** GST-India, CA-in-the-loop reconciliation pack;
  jurisdiction abstraction seam (interface, not framework) (D-007).
  **Schema shipped (DBA, 2026-07-29):** `tax_period_summary` — one table, deliberately not split into "summary" + "breakdown" (a row broken down by branch/period/gst_rate already serves both per this task's own reasoning). Computed FROM `sale`/`sale_line_item` by a future backend-developer job — not built here. RLS live-verified via the same combined migration as P2-01/P2-05/Customers above.
  **Backend implemented (2026-07-31), pending security-engineer review before Done:** `GET /api/v1/tax/summary` + `GET /api/v1/tax/export?format=csv` (ADMIN-only, ADMIN-vs-STAFF ADMIN-only per D-007), single-rate GST simplification (no per-line rate field in schema — documented in `taxService.js`), CA-review disclaimer in both response shapes, `tax_period_summary.branch_id` composite-FK gap closed (migration `0012_tax_period_summary_branch_composite_fk.sql`, same pattern as `0009`). Real-Postgres-verified (`backend/tests/tax.pgtest.mjs`). **Security review DONE (2026-07-31, `SECURITY_REVIEWS/007-tax-gst.md`): CLEAR WITH FOLLOW-UPS — no Critical/High; money-integrity (#1) and idempotent-upsert (#5) confirmed sound; 3 Low + 2 Info follow-ups tracked, none blocking. Definition-of-Done gate satisfied.**
  **Frontend wired (2026-07-31, fast-mode):** new `Tax.tsx` page (route `/tax`, `RequireRole roles={['ADMIN']}` + defensive in-component check, sidebar entry ADMIN-only) — branch selector, `periodStart`/`periodEnd` date inputs, tiles (taxable amount/tax amount/sale count/GST rate), CSV export (fetched as a blob via the authenticated `api` client, not a plain anchor, since the endpoint needs the bearer token). Disclaimer rendered as a persistent, non-dismissible amber callout above the tiles, same visual weight as `Finance.tsx`'s COGS caveat. Still blocked on the same security-engineer review as the backend before this row can move to Done.
- **Phase 7 — Deployment + branding + DR (O2/O4).**
  | P7-01 | O2 | Deploy | Docker Compose / Dokploy, long-lived pool; remove Vercel/Netlify residue | — | Backlog | — | D-006 |
  | P7-02 | O4 | Deploy | **Backups / tested restore / offsite** for 72-month retention — **named owner required** | — | Backlog | P7-01 | D-006/D-008. **NEW (CRITIQUE 016 §B.1, non-optional):** the ops runbook MUST document the `audit_log` correction escape hatch — audit rows are immutable to `grovyn_app` (SELECT/INSERT only), correctable ONLY out-of-band by the `grovyn_migrator`/superuser role, which itself is request-forbidden. Name WHO is allowed to make such a correction and under what change-control; flag the meta-gap that an operator editing `audit_log` leaves **no audit trail of having done so**. Do NOT add a UPDATE grant to `grovyn_app` to make this "convenient" — that dissolves the whole immutability guarantee. **NEW — SEC-P101-IR-02 (Medium, SECURITY_REVIEWS/002, re-affirms 001's SEC-P101-01; separate second sub-item, do NOT conflate with the audit-log runbook item above):** `backend/drizzle/bootstrap-roles.sql:27-28` ships committed placeholder passwords (`CHANGE_ME_MIGRATOR`, `CHANGE_ME_APP`) for the LOGIN **BYPASSRLS** migrator role — the crown-jewel identity. Not a P1-01 blocker (file is outside the migration chain, no reachable cluster exists yet) but **becomes High the moment a cluster is stood up**. Fix (P7 deploy + DBA): parameterize the passwords via secrets/env injection at bootstrap time so an unmodified run cannot succeed with a known password (fail-closed, not a CHANGE_ME comment); make rotation a gated runbook step. Must close before any real cluster. |
  | P7-03 | O2 | Branding | Per-tenant `runtime-config.js` branding injection | Frontend | Backlog | P7-01 | Greenfield |

## Legend for "Owner Agent"
Project Planner · Frontend Developer · Backend Developer · Database Administrator ·
Security Engineer · Decision Critic · Project Master
