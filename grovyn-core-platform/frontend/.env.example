# API base URL (origin only; no trailing slash).
# Development: leave empty to use Vite proxy to http://localhost:3001.
# For local dev, start the backend first: cd grovyn-core-platform/backend && npm run dev
# Production: set to your deployed backend (e.g. Vercel). This file is gitignored.
# VITE_API_BASE_URL=

# Production (Netlify): set in Site settings → Environment variables. No trailing slash.
# VITE_API_BASE_URL=https://grovyncopilot.vercel.app
