// Default runtime configuration (no overrides). In the Docker image, this file is
// regenerated by docker-entrypoint.sh from container environment variables at startup,
// so the same built image can serve multiple clients without a rebuild.
// See frontend/docker-entrypoint.sh and src/config/site.ts.
window.__RUNTIME_CONFIG__ = {};
